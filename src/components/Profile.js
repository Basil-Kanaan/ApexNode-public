import React from 'react';

const Profile = () => {
    return (
        <div>
            <h2>User Profile</h2>
            {/* User profile information and settings */}
        </div>
    );
};

export default Profile;
