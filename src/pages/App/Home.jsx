import React from 'react';
import { Typography } from '@mui/material';

const Home = () => {
    return (
        <div style={{ flexGrow: 1, textAlign: 'center', marginTop: '100px' }}>
            <Typography variant="h1">Hello</Typography>
        </div>
    );
};

export default Home;
